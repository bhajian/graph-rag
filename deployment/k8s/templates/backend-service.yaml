apiVersion: v1
kind: Service
metadata:
  name: langgraph-backend
  namespace: ${NAMESPACE}
spec:
  selector:
    app: langgraph-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
