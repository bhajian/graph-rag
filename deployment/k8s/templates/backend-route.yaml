apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: langgraph-backend
  namespace: ${NAMESPACE}
  annotations:
    haproxy.router.openshift.io/timeout: 120s
spec:
  to:
    kind: Service
    name: langgraph-backend
  port:
    targetPort: 8000
  tls:
    termination: edge
