apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: langgraph-frontend
  namespace: ${NAMESPACE}
  annotations:
    haproxy.router.openshift.io/timeout: 120s
spec:
  to:
    kind: Service
    name: langgraph-frontend
  port:
    targetPort: 3000
  tls:
    termination: edge
